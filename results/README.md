# Results

- `results/runs/<run_name>/` stores one experiment run, including `config.snapshot.toml`, checkpoints, predictions, and `metrics.json`.
- `results/summary/` stores aggregate CSVs and figures produced by `python -m dora_repro.cli summarize`.
